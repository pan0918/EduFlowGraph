# ─────────────────────────────────────────────────
#  EduFlowGraph — Python Dependencies
# ─────────────────────────────────────────────────

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# LLM client (OpenAI-compatible APIs)
openai>=1.0,<3.0

# Numerical computation (embedding similarity)
numpy>=1.24,<3.0

# Graph data structures (reserved for graph operations)
networkx>=3.0,<4.0
